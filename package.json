{
  "name": "cloudretail",
  "scripts": {
    "format": "prettier --ignore-unknown --write .",
    "prepare": "husky"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.3.0",
    "prettier": "^3.4.2"
  }
}
